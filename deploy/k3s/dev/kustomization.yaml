# kustomization.yaml —— 一次性 apply 全部
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: im1-dev
resources:
  - namespace.yaml
  - postgres.yaml
  - valkey.yaml
  - nats.yaml
  - im-gateway.yaml
  - ingress.yaml
  - secrets-template.yaml   # 实际用 sealed-secrets 替换
  - migrate-job.yaml
