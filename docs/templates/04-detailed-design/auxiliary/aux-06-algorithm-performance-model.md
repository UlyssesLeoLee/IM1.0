---
doc_id: aux-06
title_ja: アルゴリズム性能モデル
title_zh: 关键算法复杂度 / 性能模型
phase: 04-detailed-design-aux
owners: Tech Lead + SRE
status: Draft
version: 1.0.0
related_activities: 14 NFR, 45 逻辑设计, 80 性能试验
---

# aux-06. アルゴリズム性能モデル / 关键算法复杂度 / 性能模型

> 项目: **IM1.0**
> 阶段: 詳細設計 / Detailed Design(辅助)
> 责任方: Tech Lead + SRE

## 1. 目的 (Purpose)

为关键算法(路由 / 匹配 / 排序 / 搜索 / 加密)建立复杂度 + 性能基线,作为性能 NFR 的依据。

## 2. 适用范围 (Scope)

性能敏感的所有算法(单条消息 ≤ 10ms 的路径)。

## 3. 责任方 (Owners)

Tech Lead + SRE

## 4. 前置依赖 (Prerequisites / Inputs)

- NFR 性能(14)
- 45 逻辑设计

## 5. 输出 / 模板正文 (Body)

## A. 关键算法清单

| 编号 | 算法 | 出现位置 | 输入规模 | 性能要求 |
|---|---|---|---|---|
| A-001 | 消息路由匹配 | im-router | N 房间 × M 成员 | < 5ms |
| A-002 | 用户权限校验 | authz | 1 用户 × R 角色 | < 1ms |
| A-003 | 在线状态广播 | presence | 1 万在线 | 广播 < 1s |
| A-004 | 历史消息分页 | history | 1000 条 / 页 | < 50ms |
| A-005 | 全文搜索(可选) | search | 1 亿消息 | < 200ms |

## B. 性能模型(模板)

### A-001 消息路由匹配

**输入**:
- 房间数 N
- 房间平均成员 M
- 1 条消息

**算法**:
- Step 1:查房间元数据(Redis):O(1)
- Step 2:取成员列表(Redis):O(1)
- Step 3:并发推送(长连接):O(M) 但并行

**复杂度**:
- 时间:O(1) + O(M / 并发度)
- 空间:O(1)

**性能模型**:
- 单条消息延迟 = Redis 查(1ms) + 网络(2ms) + 推送握手(2ms) = ~5ms
- 1 万成员 = 推送握手瓶颈 → 需分片

**实测**:
- (基准 / 平均 / P99 / 峰值)

**优化策略**:
- 1. Redis Pipeline 减少 RTT
- 2. 长连接复用(同一连接多消息)
- 3. 异步推送(不阻塞发件人)

**NFR 链接**: NFR-P-001 (P99 端到端 < 200ms)

---

(其他算法同样结构)

## C. 容量 / 性能 NFR 矩阵

| 维度 | 目标 | 关联算法 |
|---|---|---|
| 端到端 P99 延迟 | < 200ms | A-001, A-002 |
| 峰值 TPS | 10k | A-001 |
| 在线用户 | 100 万 | A-003 |
| 历史查询 P99 | < 50ms | A-004 |
| 搜索 P99 | < 200ms | A-005(若有) |

## D. 实测 vs 目标

| 算法 | 目标 | 实测平均 | 实测 P99 | 状态 |
|---|---|---|---|---|
| A-001 | < 5ms | 3ms | 8ms | ⚠ 需优化 |
| A-002 | < 1ms | 0.5ms | 1.2ms | ✅ |

## E. 性能优化原则

- 测量优先:不优化未测量的代码
- 局部性:数据访问模式优先
- 批处理:减少 RTT
- 缓存:注意一致性
- 异步:不阻塞关键路径

## F. 性能回退 / 复现

- 性能回归测试:每个 PR 与 baseline 对比
- 回归超阈值:PR 阻断
- 性能基准:每周跑全量,看趋势


## 6. 验收标准 (Acceptance Criteria)

所有性能敏感算法有性能模型;实测有 baseline;性能回归 CI 阻断。

## 7. 关联文档 (References)

- 关联工程活动: 14 NFR, 45 逻辑设计, 80 性能试验
- 上游 Workflow: `docs/Workflow.md` Phase 4

## 8. 变更记录 (Change Log)

| 版本 | 日期 | 修订人 | 内容 |
|---|---|---|---|
| 1.0.0 | YYYY-MM-DD | (待定) | 初版 |
